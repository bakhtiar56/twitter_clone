import type { NextPage } from 'next'
import Head from 'next/head'
import Image from 'next/image'

const Home: NextPage = () => {
  return (
    <div>
     <Head>
        <title>Twitter</title>
     </Head>
     Hello Mom
     <main>
       {/* Sidebar */}
        {/* Feed */}
         {/* Widgets */}
     </main>
    </div>
  )
}

export default Home
